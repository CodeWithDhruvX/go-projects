name: Trigger CI/CD from Child Repo

on:
  repository_dispatch:
    types:
      - child_repo_update

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run CI/CD pipeline
        run: |
          echo "Running CI/CD process because child repo has updates"
          # Add your build and deploy steps here
